import React from 'react';

import preloader from '../../assets/image/loader.svg'

const Preloader = () => {
  return <div>
          <img src={preloader} />
        </div>
}

export default Preloader;
